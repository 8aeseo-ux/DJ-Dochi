# Server-only. Configure this in Vercel Project Settings and local .env.local.
LLM_PROVIDER=openai
OPENAI_API_KEY=
OPENAI_MODEL=

# Optional. Set only for static frontends hosted outside Vercel.
VITE_API_BASE_URL=
